import InProgress from "../components/InProgress";
import DashboardLayout from "../components/layout/DashboardLayout";

export default function Areas() {
  return (
    <DashboardLayout>
      <InProgress moduleName="el Módulo de Areas"></InProgress>
    </DashboardLayout>
  );
}